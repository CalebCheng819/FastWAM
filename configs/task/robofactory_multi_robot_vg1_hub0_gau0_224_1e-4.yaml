# @package _global_

defaults:
  - robofactory_multi_robot_ablation_224_1e-4
  - _self_

trainable_scope: dit

data:
  train:
    load_future_video: true
    gaussian_cache_dir: null
  val:
    load_future_video: true
    gaussian_cache_dir: null

model:
  training_mode: joint
  action_dit_config:
    hub_enabled: false
    enable_gaussian: false
  loss:
    lambda_video: 1.0
    lambda_action: 1.0

wandb:
  group: robofactory-multirobot-videogen-hub-gaussian-2x2x2
  name: fastwam-robofactory-vg1-hub0-gau0-s42
